export default class Inventory{
    constructor(bowls, foodBox, treats, toys, meds){
         this.bowls = bowls, 
         this.foodBox = foodBox, 
         this.treats = treats, 
         this.toys = toys, 
         this.meds = meds 
    }
}